
/*
Make two input data and two input tag to check whether the difinition of FF4 is success.
 */
val testResult = Driver(() => new FF4()) {
  c => new PeekPokeTester(c) {
    poke(c.io.in_data, 0)     // Set our input to value 0
    expect(c.io.out_data, 0)  // Assert that the output correctly has 0
    poke(c.io.in_data, 0)     // Set our input to value 0
    expect(c.io.out_data, 0)  // Assert that the output correctly has 0
    poke(c.io.in_tag, 1)     // Set our input to value 1
    expect(c.io.out_tag, 1)  // Assert that the output correctly has 1
    poke(c.io.in_tag, 1)     // Set our input to value 1
    expect(c.io.out_tag, 1)  // Assert that the output correctly has 1
    
  }
}
assert(testResult)   // Scala Code: if testResult == false, will throw an error
println("SUCCESS!!") // Scala Code: if we get here, our tests passed!
